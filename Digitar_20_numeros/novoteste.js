// Importa a biblioteca prompt-sync
const prompt = require('prompt-sync')();

// Usa a biblioteca para solicitar a entrada do usuário
const name = prompt('Qual é o seu nome? ');
console.log('Olá, '+ name);
